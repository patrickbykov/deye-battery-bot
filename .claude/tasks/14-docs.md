# 14 — Документація й бекап

**Фаза:** 4 — Продакшн-гігієна
**Залежності:** 07 (далі паралельно)
**Блокує:** нічого

## Контекст

### README розходиться з реальністю

- Описує деплой на **Render.com** (Background Worker, Build Command, Start Command). Проєкт живе на **Fly.io** — є `fly.toml`, `Dockerfile`, працюючий застосунок `deye-battery-bot`. Розділ стух приблизно на 8 комітів.
- Документує `POLL_INTERVAL` як налаштування — **його ніде не читають**. `index.js:91` має захардкоджені `setTimeout(r, 1000)`.
- Не згадує `ADMIN_CHAT_ID`, SQLite, волюм, multi-inverter і колектор.
- Інструкція локального запуску **не працює**: `cp .env.example .env && npm start`, але в проєкті немає ані `dotenv`, ані прапорця `--env-file` (на Node 18 його й не існувало). Файл `.env` просто не завантажиться.
- Заявляє MIT, але **файлу LICENSE у репо немає**.

### `.env.example` розходиться з кодом

Містить `INFLUXDB_URL`, `INFLUXDB_ORG`, `INFLUXDB_TOKEN` — **бот не читає жодну з них** (бот ходить у Influx винятково через Grafana proxy). Це змушує заводити зайвий креденшел, який нікому не потрібен, — непотрібна поверхня атаки. Після задачі 03 ці змінні стануть потрібні **колектору**, і саме там їм місце.

Відсутні `PORT` (`config.js:8`) і `ADMIN_CHAT_ID` (`config.js:9`).

### Бекапу БД немає

**Оновлено 4 вер 2026: після фази 5 бекап переходить з «бажано» в
«обов'язково».** У БД тепер не лише підписки, а й **рішення про доступ до
чужих будинків**: втрата волюма означає, що всі стають `pending` і адмін
схвалює всіх заново, не пам'ятаючи, кому що дозволяв. `/export` має
вивантажувати `users` разом із `subscriptions`. Деталі — задача 24.

`data/bot.db` на Fly-волюмі. **Підписки — єдиний невідновлюваний стан у системі**: інвертори перевідкриються з InfluxDB через discovery, алерти живуть у Grafana, метрики в Influx. Підписки не відновить ніщо. Fly-волюм прив'язаний до одного хоста; втрата машини = втрата всіх підписок без шляху відновлення.

## Кроки

1. Переписати README:
   - архітектура: **інвертор → Deye Cloud → колектор → InfluxDB → Grafana → бот**, з місцем алертів (Grafana, не бот)
   - деплой на Fly замість Render: `fly volumes create`, `fly secrets set`, `fly deploy`
   - актуальний список команд з фази 3
   - робоча інструкція локального запуску (`node --env-file=.env index.js` на Node 20+, або додати `dotenv`)
   - згадати два застосунки: `deye-battery-bot` і `deye-collector`
2. Розділити env-змінні: `.env.example` для бота (прибрати `INFLUXDB_URL`/`ORG`/`TOKEN` і `POLL_INTERVAL`, додати `PORT` і `ADMIN_CHAT_ID`), `collector/.env.example` — окремо.
3. Або реалізувати `POLL_INTERVAL`, або прибрати з документації. Документована, але неіснуюча опція гірша за відсутню.
4. Додати файл `LICENSE` (MIT), раз він заявлений.
5. Бекап SQLite:
   - періодичний `VACUUM INTO '/app/data/backup-<date>.db'` з ротацією, або
   - експорт підписок у JSON і вивантаження назовні
   Найпростіше — окрема адмінська команда `/export`, що шле дамп підписок у чат адміна. Мінімум зусиль, дає відновлення після втрати волюма.
6. Прибрати мертві гілки: `list-invertors-support` дублює `main`, `origin/flyio-new-files` має відкритий PR #1 і відстає на 5 комітів. Або домержити, або закрити.

## Критерії готовності

- [x] README описує реальний деплой (Fly) і реальну архітектуру з колектором
- [x] Інструкція локального запуску з README **виконана й працює**
- [x] `.env.example` збігається з `grep -rn "process.env" *.js`; `collector/.env.example` — з `grep -rn "process.env" collector/`
- [x] Обидва файли покривають усі секрети, виставлені у Fly (і навпаки — зайвих немає)
- [x] `POLL_INTERVAL` або працює, або не згадується ніде
- [x] `LICENSE` у репо
- [x] Бекап підписок відпрацював хоча б раз, відновлення перевірено на порожній БД
- [x] Гілки прибрані


## Результат (4 вер 2026)

README переписаний під реальну архітектуру: два застосунки, шлях алертів через
webhook, правило «не масштабувати понад одну машину», і те, що зміна підписок
коштує повторного схвалення. `LICENSE` (MIT) додано — його було заявлено, але
не існувало.

`.env.example` розділено по застосунках і звірено з `process.env` в обидва боки.
`POLL_INTERVAL` прибрано звідусіль: документована, але неіснуюча опція гірша за
відсутню.

Інструкція локального запуску **виконана й працює**:
```
node --env-file=.env index.js
→ HTTP на порту 8093, health: 200 OK
```

Бекап зроблено в задачі 24: щодобовий `VACUUM INTO` з ротацією на 7,
`/export` у Telegram, `GET /admin/export.json`.

### Лишається за рішенням власника

Мертві гілки: локальні `feature/multi-inverter` і `list-invertors-support`,
плюс `origin/flyio-new-files` з відкритим PR #1. Видалення гілок і закриття
PR — незворотні дії в публічному репозиторії, тож не робив.
